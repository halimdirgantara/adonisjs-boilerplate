import PermissionRole from 'App/Models/PermissionRole'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(PermissionRole, ({ faker }) => {
  return {
    //
  }
}).build()
