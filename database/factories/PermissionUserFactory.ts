import PermissionUser from 'App/Models/PermissionUser'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(PermissionUser, ({ faker }) => {
  return {
    //
  }
}).build()
